#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

#define BUFSI2E 200

int main(int argc, char *argv[]) {

  struct stat infos;
  if( fstat(STDIN_FILENO, &infos) < 1)
    







}
